export const emptyFieldMsg = (field: string): string => {
  return `Campo ${field} em branco`
}

export const idNotFoundMsg = (): string => {
  return 'Id não encontrado'
}

export const musicNotFoundMsg = (): string => {
  return 'Musica não encontrada'
}

export const serverErrorMsg = (): string => {
  return 'Server error'
}
